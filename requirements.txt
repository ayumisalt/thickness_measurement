# Python-only environment (for pyenv/venv users).
# ROOT/C++ dependencies are intentionally provided by environment.yml instead.
numpy>=1.26,<3
scipy>=1.11,<2
matplotlib>=3.8,<4
opencv-python-headless>=4.8,<5
pytest>=8,<9
